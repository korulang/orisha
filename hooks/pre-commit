#!/usr/bin/env bash
# Thin shim → runs the CommonJS gate. The .cjs extension forces CommonJS so the
# hook works regardless of the repo's package.json "type".
exec node "$(dirname "$0")/pre-commit.cjs" "$@"
